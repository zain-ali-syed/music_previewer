import React from 'react';

const Search = () => {
    return (
        <form >
            <input type="search" />
            <i class="fa fa-search"></i>
        </form>
    );
};

export default Search;